spring.application.name=eureka-client-service
#server.port=8081
server.port=0

pring.datasource.driver-class-name = org.sqlite.JDBC
spring.datasource.url = jdbc:sqlite:sample.db
spring.datasource.username = 
spring.datasource.password = 
spring.datasource.testWhileIdle=true
spring.datasource.validationQuery=SELECT 1

spring.session.jdbc.initialize-schema=always

eureka.client.registerWithEureka=true
eureka.client.fetchRegistry=true
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/


#eureka.instance.hostname=localhost
#eureka.instance.instanceId: ${eureka.instance.hostname}:${spring.application.name}

#eureka.instance.statusPageUrl=http://${eureka.hostname}/
#eureka.instance.healthCheckUrl=http://${eureka.hostname}/actuator/health